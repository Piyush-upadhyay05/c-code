// write the program to check the number where it is divisible by 97 or not?
#include <stdio.h>

int main(){
    int a = 33467;
    printf("the value of a%97 is %d", a%97);
    return 0;
}
// if reminder is 0 then the is divisible by 97 completely 